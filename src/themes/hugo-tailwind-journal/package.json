{
  "scripts": {
    "watch": "cross-env NODE_ENV=development postcss scss/*.css --dir static/css/ --ext css --watch",
    "build": "cross-env NODE_ENV=production postcss scss/*.css --dir static/css/ --ext css"
  },
  "dependencies": {
    "autoprefixer": "^9.5.1",
    "postcss-import": "^12.0.1",
    "tailwindcss": "^1.0"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.2.0",
    "concurrently": "^4.1.0",
    "cross-env": "^5.2.0",
    "cssnano": "^4.1.10",
    "postcss-cli": "^6.1.2"
  }
}
